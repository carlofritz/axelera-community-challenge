# Booth Answers Log

Use this file to capture authoritative answers from Axelera/Ultralytics contacts.

## Required Questions
1. Does pose decode support non-COCO keypoint shapes (e.g., 21x3) in fast pipeline mode?
2. Is there an official hand-keypoint YAML/pipeline example for Voyager?
3. What is the fastest recommended path for demo: custom weights vs direct export workflow?
4. Which mode is best on Orange Pi 5 Plus for time-to-demo: `torch-aipu` or `gst`?
5. What exactly is meant by "LLM-assisted kernels" in SDK context?

## Capture Format
- Date:
- Contact:
- SDK version / branch:
- Question:
- Answer:
- Verification pointer (doc path, repo path, issue link):
- Impacted step IDs:
